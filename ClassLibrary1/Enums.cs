using System;

namespace Common
{
    public enum DeletionStatus
    {
        Deleted = 0,
        NotDeleted
    }

    public enum Difficulty
    {
        None = 0,
        Easy, Medium, Hard
    }
}
